name: Docker Build

on:
  push:
    branches: [main]
    paths:
      - 'docker/Dockerfile'
      - 'pyproject.toml'
  pull_request:
    branches: [main]
    paths:
      - 'docker/Dockerfile'
      - 'pyproject.toml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3

      - name: Build image
        run: docker build -f docker/Dockerfile -t orchard-ml:ci .
